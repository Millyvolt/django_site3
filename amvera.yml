---
version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: requirements.txt
run:
  command: uvicorn mysite.asgi:application --host 127.0.0.1 --port 8000 --reload
  persistenceMount: /data
  containerPort: 8000
